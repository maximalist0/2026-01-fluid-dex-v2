export const ratesAuthConfig = {
  percentRateChangeAllowed: 20 * 1e2, // maximum amount percent of rate change allowed in a given timeframe (relative!)
  cooldown: 1, // cooldown time until next rate change is possible again
};
